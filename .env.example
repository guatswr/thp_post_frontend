THP_DOMAIN=post.example.com
ACME_EMAIL=admin@example.com
# 同一 Docker 主机且共用 thp_post_net 时保持默认值
THP_BACKEND_UPSTREAM=http://thp-post-backend:8000
# 国内服务器按需启用 Docker Hub 镜像站；只写域名，不带 https://
# DOCKER_REGISTRY=docker.1panel.live
